doctype html
html
  head
    title Natours
    link(rel='stylesheet', href='/css/style.css')
    //- script(type="module", src="/javascript/natours.js")
    //- script(src=`https://maps.googleapis.com/maps/api/js?key=${process.env.MAP_KEY}&libraries=places`)

  body
    h2= tour.name
    p= tour.price
    .map
